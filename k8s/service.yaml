apiVersion: v1
kind: Service
metadata:
  name: interview-prep-service
spec:
  type: NodePort
  selector:
    app: interview-prep
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30080
